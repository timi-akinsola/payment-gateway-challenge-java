Checkout.com Take-Home Exercise
Payment Gateway Challenge

By Timilehin Akinsola
Started on 23rd September 2026

---

CHANGELOG: 

1. Added Bean Validation to Gradle Build Info (build-info).
2. Deleted GetPaymentResponse and renamed PostPaymentResponse to PaymentResponse to consolidate Response model to one type (as they were identical before anyway).
3. Renamed PostPaymentRequest to PaymentRequest to simplify the relation between PaymentRequest and PaymentResponse.
4. Renamed card_number_last_four to card_number in PaymentRequest to capture the full number. Left PaymentResponse as-is.
5. Changed card_number and cvv from int to String in PaymentRequest (along with card_number_last_four in PaymentResponse) to natively handle leading and trailing zeros.
6. Reformatted expiry date to always be 2 digits for expiry day & month (accounting for leading zeroes.
	- NOTE: Maybe remove this later, as expiry date shouldn't be inbound request information as opposed to outbound response information?
7. Added NotBlank and Regex constraint to cardNumberLastFour
8. Added Min & Max range constraint to expiryMonth. 
	- NOTE: Would use Range but that's from the Hibernate package while all other validators are from Jakarta, so let's minimise dependencies.
9. Changed expiryMonth and expiryYear to Integer and added NotNull constraint, to make it more clear that the values cannot be null/blank as ints.
10. Added NotBlank and Regex constraint to currency.
	- UPDATE: Changed regex from "[A-Z]{3}" to "USD|GBP|EUR" to enforce currency to only accept USD, GBP, and EUR at the moment.
11. Added Positive constraint to amount.
12. Added NotBlank and Pattern constraint to CVV.
13. Removed CVV from PaymentRequest.toString() to avoid exposing sensitive data.

14. Turned PaymentsRepository into an interface and created an InMemoryPaymentsRepository class to implement it, to allow for scalability with other data access mechanisms (Postgres, Redis, etc.) while utilising dependency injection.
15. Created a new PaymentNotFoundException to more accurately categorise exceptions related to PaymentsRepository (e.g. incorrectly input card number).

16. Created BankPaymentRequest and BankPaymentResponse classes to represent data sent to & from the Mountebank simulator, including a computed expiryDate property in the request class.
	- Left authorize() with American spelling to match PaymentStatus enum.
17. Created BankClient to handle the authorisation of BankPaymentRequests, and added the Mountebank simulator URL to application.properties for centralised config.
18. Created BankServiceException to handle bank-side errors (e.g. bank unavailable).
19. Created BankClientTest and wrote 4 unit test cases to handle different outcomes of the bank payment use case.

20. Removed "api" parameter from RestController in PaymentGatewayController as it's unneeded.
21. Renamed "payment/" API endpoints to "payments/" in PaymentGatewayController to match the Mountebank endpoints.
22. Implemented processPayment in PaymentGatewayService using BankClient via injection.
23. Exposed a POST endpoint in PaymentGatewayController to handle the processing of PaymentRequests via PaymentGatewayService.
24. Extended ErrorResponse and added a MethodArgumentNotValidException case in CommonExceptionHandler, along with a @Valid keyword in PaymentGatewayController to raise grouped validation errors when processing PaymentRequests.

25. Added @AssertTrue annotation for expiry date in PaymentRequest to validate that the date is not in the past.

26. Added a suite of Testcontainers integration tests to test user stories from end to end.

27. Removed EventProcessingException as it's not used.
28. Added SpotBugs, Spotless, Dependabot, and DependencyManagement plugins for code cleanliness, and created a CI/CD pipeline YAML to handle running the Testcontainers tests in a CI environment.

GENERAL NOTES: 
- Changelogs are manually updated -- could import a library (e.g. https://github.com/spring-io/github-changelog-generator) or use implement an AI skill/prompt to autonomously generate a changelog for each commit instead.
	- This was done so I had a clearer picture on my progress and rationale behind my actions at each step, in case I thought of a better refactor/optimisation approach later on.